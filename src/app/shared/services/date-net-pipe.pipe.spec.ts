import { DateNetPipePipe } from './date-net-pipe.pipe';

describe('DateNetPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateNetPipePipe();
    expect(pipe).toBeTruthy();
  });
});
